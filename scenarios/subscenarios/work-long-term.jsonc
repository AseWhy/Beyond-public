{
    "name": "Работа-долгосрочная",
    "id": "work-long-term",
    "use_state": true,
    "use_character": true,
    "import": ["businesses.lib"],
    "preset": {
        "ended": null
     },
    "condition": "false",
    "entries": [
        {
            "type": "write",
            "break_on": true,
            "prepare": "set work = businesses.getWorkFullInfoBy('id', character.data.cur_work_ident, character);",
            "message": [
                "[СИСТЕМА]\n",
                "Персонаж, в данный момент, работает. Невозможно обработать данную команду в текущем контексте. До завершения осталось {> numbers.to_ru_time_value(state.data.ended - date.now())}."
            ],
            "condition": "work = null"
        },
        {
            "type": "save",
            "entries": { 
                "ended": "date.now() + work.duration"
            },
            "condition": "true"
        }
    ]
}